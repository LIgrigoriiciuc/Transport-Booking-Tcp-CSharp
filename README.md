# Transport-Booking-Tcp-Udp-CSharp
Desktop client-server reservation system for a transport company, built in C#. Uses TCP for reliable communication and UDP for real-time notifications to all connected clients on reservation changes. TCP version available on the tcp branch. Interoperable with the Java version.
